apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cnpg-cluster
  namespace: dev
spec:
  instances: 1
  bootstrap:
    initdb:
      database: devops_db
      owner: app
  storage:
    size: 1Gi